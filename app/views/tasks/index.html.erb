<div class="container-t">
  <h1>Tasks</h1>

  <ul>
    <% @tasks.each do |t| %>
      <li> ☐ <%= link_to t.title, task_path(t) %> </li>
    <% end %>
  </ul>

  + <%= link_to "Add a new task", new_task_path %>

</div>
